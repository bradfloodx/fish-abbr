# shell-abbreviations

```sh
abbr -a pull "git pull --rebase"
abbr -a gfo "git fetch origin"
abbr -a gfop "git fetch origin --prune"
abbr -a c "clear"
abbr -a lsn "ls -lah -la node_modules | grep "\->""
abbr -a stash "git stash"
abbr -a ch "git checkout"
abbr -a chb "git checkout -b"
abbr -a gchb "git checkout -b"
abbr -a gaa "git add -A"
abbr -a ga "git add"
abbr -a gcm "git commit -m"
abbr -a gcam "git commit -am"
abbr -a push "git push -u"
abbr -a reset "git reset HEAD~"
abbr -a www "cd ~/www"
abbr -a pop "git stash pop"
abbr -a diff "git diff"
abbr -a gb "git branch -a"
abbr -a pushn "git push --no-verify"
abbr -a m "git merge"
abbr -a desktop "cd /Users/bflood/Desktop/"
abbr -a gbd "git branch -d"
abbr -a watch "nodemon --watch ./src --watch ./src --ext js,json,handlebars --exec exec-notify npm compile-dev"
abbr -a clone "git clone"
abbr -a na "node app.js"
abbr -a g "git status -sb"
abbr -a nl "npm link"
abbr -a pushf "git push -uf"
abbr -a nu "ynpm update"
abbr -a test "nodemon --watch ./tests --watch src --ext js --exec './node_modules/.bin/jenkins-mocha --recursive './tests/unit/**/*.js' --bail'"
abbr -a nul "npm unlink"
abbr -a ws "webstorm ./"
```
